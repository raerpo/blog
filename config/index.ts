/**
 * Folder that will serve the static content
 */
export const CONTENT_PATH = `${process.cwd()}/public/`;

/**
 * Folder that contains the markdown files that will be compiled to HTML
 */
export const DATA_PATH = `${process.cwd()}/data`;

/**
 * Folder in which the templates live
 */
export const TEMPLATES_PATH = `${process.cwd()}/templates`;

/**
 * How much of post body will be shown on the index
 */
export const BODY_PREVIEW_LENGTH = 150;